---
layout: news
name: "День 2: Переменные и типы данных в PHP"
title: "PHP обучение с нуля: День 2 - Переменные, типы данных и работа с информацией"
description: "Изучаем переменные в PHP, основные типы данных, работу со строками и числами. Практические примеры для реальных проектов."
date: 2026-01-20
image: "/images/learn-php/learn-php01.webp"
category: php
news_id: 8c52679120b947c3
---

<div class="goal-box">
<h3 class="goal-title">🎯 Цель урока</h3>
<p>Научиться работать с переменными, понимать основные типы данных в PHP, использовать var_dump() для отладки и освоить концепцию динамической типизации. Научиться применять эти знания в реальных проектах.</p>
</div>

<section class="section theory">
<h2 class="section-title">📚 Теоретическая часть (45 минут)</h2>

<div class="card">
<h3><span class="icon">📦</span> Что такое переменные и зачем они нужны?</h3>

<p><strong>Переменная</strong> — это как контейнер для хранения информации. Представьте, что вы заказываете пиццу. Вы говорите курьеру: "Оставьте заказ у <strong>двери</strong>". Слово "дверь" — это переменная, а конкретная дверь вашей квартиры — это значение.</p>

<div class="key-points">
<h4>💡 Почему переменные — это основа программирования?</h4>
<ul>
<li><strong>Хранение данных</strong> — имена пользователей, цены товаров, настройки сайта</li>
<li><strong>Повторное использование</strong> — один раз записали, много раз использовали</li>
<li><strong>Изменяемость</strong> — можно обновлять значения (скидки, остатки товаров)</li>
<li><strong>Читаемость кода</strong> — <code>$total_price</code> понятнее, чем просто число 1499</li>
</ul>
</div>

<div class="comparison-table table--hover">
<table>
<caption>Переменные в реальной жизни</caption>
<thead>
<tr>
<th>Реальная ситуация</th>
<th>PHP-переменная</th>
<th>Преимущество</th>
</tr>
</thead>
<tbody>
<tr>
<td>Цена товара в интернет-магазине</td>
<td><code>$product_price = 2990;</code></td>
<td>Легко изменить при распродаже</td>
</tr>
<tr>
<td>Имя пользователя в форме</td>
<td><code>$user_name = "Анна";</code></td>
<td>Используется в приветствии, email, профиле</td>
</tr>
<tr>
<td>Количество товаров в корзине</td>
<td><code>$cart_items = 3;</code></td>
<td>Автоматически обновляется при добавлении/удалении</td>
</tr>
<tr>
<td>Статус заказа</td>
<td><code>$order_status = "processing";</code></td>
<td>Можно менять: processing → shipped → delivered</td>
</tr>
</tbody>
</table>
</div>
</div>

<div class="card">
<h3><span class="icon">🏷️</span> Правила именования переменных</h3>

<div class="key-points">
<h4>✅ Правильно:</h4>
<div class="example-grid">
<div class="correct-example">
<pre><code>$user_name = "Иван";     // понятное имя
$item_count = 5;        // описывает содержание
$totalAmount = 1000;    // camelCase стиль
$_private_data = "xxx"; // начинается с подчеркивания
$is_active = true;      // булева переменная
$price_with_vat = 1200; // snake_case стиль</code></pre>
</div>
</div>

<h4>❌ Неправильно:</h4>
<div class="incorrect-example">
<pre><code>$a = "Иван";            // что означает "а"?
$user name = "Иван";   // пробел в имени
$2users = 5;           // начинается с цифры
$имя = "Иван";         // кириллица (работает, но не рекомендуется)
$user-name = "Иван";   // дефис в имени
$echo = "текст";       // зарезервированное слово</code></pre>
</div>
</div>

<div class="note">
<p><strong>💡 Практический совет:</strong> Называйте переменные так, чтобы через месяц вы могли понять, что в них хранится. <code>$total_order_amount</code> лучше, чем <code>$toa</code> или <code>$x</code>.</p>
</div>
</div>

<div class="card">
<h3><span class="icon">🎭</span> Типы данных в PHP</h3>
<p>PHP автоматически определяет тип данных, но понимать их важно для правильной работы.</p>

<div class="data-types-grid">
<div class="data-type int">
<h4>1. Целые числа (int)</h4>
<p><strong>Что хранит:</strong> Возраст, количество, ID, голоса, рейтинг</p>
<pre><code>$age = 25;          // возраст пользователя
$quantity = 10;     // количество товаров
$user_id = 15487;   // уникальный идентификатор
$votes = 234;       // количество голосов</code></pre>
<p><strong>Боевой пример:</strong> Подсчет товаров в корзине, голосование, пагинация страниц.</p>
</div>

<div class="data-type float">
<h4>2. Числа с плавающей точкой (float)</h4>
<p><strong>Что хранит:</strong> Цены, проценты, координаты, рейтинги с дробями</p>
<pre><code>$price = 2999.99;    // цена товара
$discount = 15.5;   // процент скидки
$rating = 4.7;      // рейтинг товара
$latitude = 55.7558; // координаты</code></pre>
<p><strong>Боевой пример:</strong> Расчет итоговой суммы с НДС, вычисление скидок, карты и геолокация.</p>
</div>

<div class="data-type string">
<h4>3. Строки (string)</h4>
<p><strong>Что хранит:</strong> Текст, имена, email, пароли, URL, JSON</p>
<pre><code>$name = "Анна Иванова";
$email = "anna@mail.com";
$message = "Привет, $name!";
$json_data = '{"id":1,"name":"Test"}';</code></pre>
<p><strong>Боевой пример:</strong> Формирование email-рассылок, обработка форм, генерация XML/JSON для API.</p>
</div>

<div class="data-type bool">
<h4>4. Булевы значения (bool)</h4>
<p><strong>Что хранит:</strong> Флаги состояния: да/нет, вкл/выкл, активно/неактивно</p>
<pre><code>$is_logged = true;   // пользователь авторизован
$has_access = false; // нет доступа к разделу
$is_admin = true;    // права администратора
$is_active = false;  // пользователь заблокирован</code></pre>
<p><strong>Боевой пример:</strong> Проверка прав доступа, включение/выключение функций сайта, модерация контента.</p>
</div>

<div class="data-type null">
<h4>5. NULL</h4>
<p><strong>Что хранит:</strong> Отсутствие значения, неинициализированные данные</p>
<pre><code>$middle_name = null;  // отчество не указано
$avatar = null;      // аватар не загружен
$last_login = null;  // первый вход пользователя
$data = null;        // данные еще не получены</code></pre>
<p><strong>Боевой пример:</strong> Проверка заполненности полей формы, обработка отсутствующих данных из API.</p>
</div>
</div>
</div>

<div class="card">
<h3><span class="icon">🔄</span> Динамическая типизация PHP</h3>

<p><strong>Динамическая типизация</strong> — это когда PHP сам определяет тип данных и позволяет его менять. Это как универсальный контейнер, в который можно положить что угодно.</p>

<div class="key-points">
<h4>🔧 Практическое применение:</h4>
<ul>
<li><strong>Гибкость форм</strong> — поле "телефон" может быть и строкой, и числом</li>
<li><strong>Работа с API</strong> — один endpoint может возвращать разные типы данных</li>
<li><strong>Универсальные функции</strong> — одна функция может обрабатывать строки и числа</li>
<li><strong>Быстрое прототипирование</strong> — не нужно объявлять типы заранее</li>
</ul>
</div>

<div class="note">
<p><strong>⚠️ Важно:</strong> Динамическая типизация — это и сила, и слабость PHP. Она дает гибкость, но требует внимательности — можно случайно изменить тип данных и получить ошибку.</p>
</div>
</div>

<div class="card">
<h3><span class="icon">🔍</span> var_dump() — ваш лучший друг для отладки</h3>

<p>Когда что-то не работает, <code>var_dump()</code> покажет, что на самом деле хранится в переменной.</p>

<div class="comparison-table table--small">
<table>
<caption>echo vs var_dump()</caption>
<thead>
<tr>
<th>echo</th>
<th>var_dump()</th>
<th>Когда использовать</th>
</tr>
</thead>
<tbody>
<tr>
<td>Показывает только значение</td>
<td>Показывает тип, значение и размер</td>
<td>Для отладки и понимания проблемы</td>
</tr>
<tr>
<td><code>1499</code></td>
<td><code>int(1499)</code></td>
<td>Когда нужно знать точный тип</td>
</tr>
<tr>
<td><code>true</code> → "1"</td>
<td><code>bool(true)</code></td>
<td>Для булевых значений</td>
</tr>
<tr>
<td><code>null</code> → пустота</td>
<td><code>NULL</code></td>
<td>Для проверки на null</td>
</tr>
</tbody>
</table>
</div>

<div class="key-points">
<h4>🎯 Боевой пример использования var_dump():</h4>
<pre><code>// Проблема: форма не отправляется, поле "возраст" пустое
$age = $_POST['age']; // что пришло из формы?

// Вместо того чтобы гадать:
var_dump($age); 
// Результат: string(0) "" 
// Ага! Пришла пустая строка, а не число!
// Решение: проверить на пустоту перед использованием</code></pre>
</div>
</div>
</section>

<section class="section practice">
<h2 class="section-title">💻 Практическая часть (1.5 часа)</h2>

<div class="task">
<h3><span class="icon">✅</span> Подготовка рабочего файла</h3>

<ol>
<li>В папке <code>php_course</code> создайте файл <code>day2.php</code></li>
<li>Добавьте базовую структуру:
<pre><code>&lt;!DOCTYPE html&gt;
&lt;html lang="ru"&gt;
&lt;head&gt;
&lt;meta charset="UTF-8"&gt;
&lt;title&gt;День 2: Переменные в PHP&lt;/title&gt;
&lt;style&gt;
body { font-family: Arial; max-width: 1000px; margin: 40px auto; }
.dump { background: #2c3e50; color: white; padding: 10px; border-radius: 5px; }
.example { background: #f8f9fa; padding: 15px; margin: 15px 0; border-left: 3px solid #3498db; }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;h1&gt;Работа с переменными&lt;/h1&gt;
&lt;?php
// Здесь будем писать код
?&gt;
&lt;/body&gt;
&lt;/html&gt;</code></pre>
</li>
</ol>
</div>

<div class="task">
<h3><span class="icon">✅</span> Задание 1: Регистрация пользователя (боевой пример)</h3>

<p>Создайте переменные для формы регистрации на сайте:</p>

<pre><code>&lt;?php
// Данные из формы регистрации
$first_name = "Анна";
$last_name = "Иванова";
$email = "anna@example.com";
$age = 28; // целое число
$subscribe = true; // подписка на рассылку
$middle_name = null; // отчество не указано

echo "&lt;h2&gt;Регистрация нового пользователя&lt;/h2&gt;";
echo "&lt;div class='example'&gt;";
echo "&lt;p&gt;&lt;strong&gt;Имя:&lt;/strong&gt; $first_name&lt;/p&gt;";
echo "&lt;p&gt;&lt;strong&gt;Фамилия:&lt;/strong&gt; $last_name&lt;/p&gt;";
echo "&lt;p&gt;&lt;strong&gt;Email:&lt;/strong&gt; $email&lt;/p&gt;";
echo "&lt;p&gt;&lt;strong&gt;Возраст:&lt;/strong&gt; $age лет&lt;/p&gt;";
echo "&lt;p&gt;&lt;strong&gt;Подписка:&lt;/strong&gt; " . ($subscribe ? "Да" : "Нет") . "&lt;/p&gt;";
echo "&lt;/div&gt;";

// Проверка с помощью var_dump()
echo "&lt;h3&gt;Отладочная информация (var_dump):&lt;/h3&gt;";
echo "&lt;div class='dump'&gt;";
echo "&lt;pre&gt;";
var_dump($first_name); // string(8) "Анна"
var_dump($age);        // int(28)
var_dump($subscribe);  // bool(true)
var_dump($middle_name);// NULL
echo "&lt;/pre&gt;";
echo "&lt;/div&gt;";
?&gt;</code></pre>

<div class="note">
<p><strong>💡 Боевой сценарий:</strong> Так выглядит обработка данных из реальной формы регистрации. Эти переменные потом будут сохранены в базу данных, использованы для отправки приветственного email и создания сессии пользователя.</p>
</div>
</div>

<div class="task">
<h3><span class="icon">✅</span> Задание 2: Интернет-магазин (расчет корзины)</h3>

<p>Создайте переменные для корзины покупок:</p>

<pre><code>&lt;?php
// Данные товаров в корзине
$product1_name = "Ноутбук";
$product1_price = 45999.99;
$product1_quantity = 1;

$product2_name = "Мышь беспроводная";
$product2_price = 2499.50;
$product2_quantity = 2;

$product3_name = "Коврик для мыши";
$product3_price = 799.00;
$product3_quantity = 1;

// Расчеты
$subtotal1 = $product1_price * $product1_quantity;
$subtotal2 = $product2_price * $product2_quantity;
$subtotal3 = $product3_price * $product3_quantity;

$total_items = $product1_quantity + $product2_quantity + $product3_quantity;
$total_amount = $subtotal1 + $subtotal2 + $subtotal3;

// Боевой пример: применение скидки
$discount_percent = 10; // скидка 10%
$discount_amount = $total_amount * ($discount_percent / 100);
$final_amount = $total_amount - $discount_amount;

echo "&lt;h2&gt;Корзина покупок&lt;/h2&gt;";
echo "&lt;div class='example'&gt;";

echo "&lt;table border='1' cellpadding='10'&gt;";
echo "&lt;tr&gt;&lt;th&gt;Товар&lt;/th&gt;&lt;th&gt;Цена&lt;/th&gt;&lt;th&gt;Кол-во&lt;/th&gt;&lt;th&gt;Сумма&lt;/th&gt;&lt;/tr&gt;";
echo "&lt;tr&gt;&lt;td&gt;$product1_name&lt;/td&gt;&lt;td&gt;$product1_price руб.&lt;/td&gt;&lt;td&gt;$product1_quantity&lt;/td&gt;&lt;td&gt;$subtotal1 руб.&lt;/td&gt;&lt;/tr&gt;";
echo "&lt;tr&gt;&lt;td&gt;$product2_name&lt;/td&gt;&lt;td&gt;$product2_price руб.&lt;/td&gt;&lt;td&gt;$product2_quantity&lt;/td&gt;&lt;td&gt;$subtotal2 руб.&lt;/td&gt;&lt;/tr&gt;";
echo "&lt;tr&gt;&lt;td&gt;$product3_name&lt;/td&gt;&lt;td&gt;$product3_price руб.&lt;/td&gt;&lt;td&gt;$product3_quantity&lt;/td&gt;&lt;td&gt;$subtotal3 руб.&lt;/td&gt;&lt;/tr&gt;";
echo "&lt;/table&gt;";

echo "&lt;h3&gt;Итог:&lt;/h3&gt;";
echo "&lt;p&gt;Товаров в корзине: $total_items шт.&lt;/p&gt;";
echo "&lt;p&gt;Общая сумма: $total_amount руб.&lt;/p&gt;";
echo "&lt;p&gt;Скидка ($discount_percent%): -$discount_amount руб.&lt;/p&gt;";
echo "&lt;p&gt;&lt;strong&gt;К оплате: $final_amount руб.&lt;/strong&gt;&lt;/p&gt;";
echo "&lt;/div&gt;";

// Отладочный вывод типов
echo "&lt;h3&gt;Типы данных в расчетах:&lt;/h3&gt;";
echo "&lt;div class='dump'&gt;";
echo "&lt;pre&gt;";
echo "product1_price: "; var_dump($product1_price); // float(45999.99)
echo "product1_quantity: "; var_dump($product1_quantity); // int(1)
echo "subtotal1: "; var_dump($subtotal1); // float(45999.99)
echo "discount_percent: "; var_dump($discount_percent); // int(10)
echo "discount_amount: "; var_dump($discount_amount); // float(5549.848)
echo "&lt;/pre&gt;";
echo "&lt;/div&gt;";
?&gt;</code></pre>

<div class="note">
<p><strong>💰 Боевой сценарий:</strong> Именно так работает корзина любого интернет-магазина. Переменные хранят цены, количество, рассчитывают итоги. В реальном проекте эти данные берутся из базы данных и сессии пользователя.</p>
</div>
</div>

<div class="task">
<h3><span class="icon">✅</span> Задание 3: Работа со строками (генерация email)</h3>

<p>Создайте переменные для отправки email-уведомления:</p>

<pre><code>&lt;?php
// Данные для email
$customer_name = "Сергей Петров";
$order_id = 15678;
$order_date = "22.01.2026";
$delivery_date = "25.01.2026";
$tracking_number = "RU123456789";
$support_email = "support@myshop.ru";

// Конкатенация строк (объединение)
$subject = "Заказ №" . $order_id . " подтвержден";
$greeting = "Уважаемый(ая) " . $customer_name . ",";

// Интерполяция строк (вставка переменных внутрь)
$body = "Ваш заказ №$order_id от $order_date подтвержден.
Ожидаемая дата доставки: $delivery_date
Трек-номер для отслеживания: $tracking_number

По всем вопросам обращайтесь на $support_email";

// HTML письмо (реальный пример)
$html_body = "&lt;html&gt;
&lt;body style='font-family: Arial, sans-serif;'&gt;
&lt;h2&gt;Заказ №$order_id подтвержден&lt;/h2&gt;
&lt;p&gt;Уважаемый(ая) $customer_name,&lt;/p&gt;
&lt;p&gt;Ваш заказ от &lt;strong&gt;$order_date&lt;/strong&gt; подтвержден.&lt;/p&gt;
&lt;div style='background: #f8f9fa; padding: 15px; margin: 15px 0;'&gt;
&lt;p&gt;&lt;strong&gt;Ожидаемая дата доставки:&lt;/strong&gt; $delivery_date&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Трек-номер:&lt;/strong&gt; $tracking_number&lt;/p&gt;
&lt;/div&gt;
&lt;p&gt;По всем вопросам обращайтесь на &lt;a href='mailto:$support_email'&gt;$support_email&lt;/a&gt;&lt;/p&gt;
&lt;/body&gt;
&lt;/html&gt;";

echo "&lt;h2&gt;Email-уведомление для клиента&lt;/h2&gt;";
echo "&lt;div class='example'&gt;";
echo "&lt;p&gt;&lt;strong&gt;Тема:&lt;/strong&gt; $subject&lt;/p&gt;";
echo "&lt;pre style='background: white; padding: 15px; border: 1px solid #ddd;'&gt;$body&lt;/pre&gt;";
echo "&lt;/div&gt;";

echo "&lt;h3&gt;HTML версия письма:&lt;/h3&gt;";
echo "&lt;div style='border: 1px solid #ddd; padding: 20px; background: white;'&gt;";
echo $html_body;
echo "&lt;/div&gt;";

// Отладка
echo "&lt;h3&gt;Отладочная информация:&lt;/h3&gt;";
echo "&lt;div class='dump'&gt;";
echo "&lt;pre&gt;";
echo "customer_name: "; var_dump($customer_name);
echo "order_id: "; var_dump($order_id);
echo "Длина subject: " . strlen($subject) . " символов\n";
echo "&lt;/pre&gt;";
echo "&lt;/div&gt;";
?&gt;</code></pre>

<div class="note">
<p><strong>📧 Боевой сценарий:</strong> Так создаются автоматические email-уведомления в интернет-магазинах. В реальном проекте этот код выполняется после успешного оформления заказа и отправляет письмо через mail() или SMTP-библиотеку.</p>
</div>
</div>

<div class="task">
<h3><span class="icon">✅</span> Задание 4: Динамическая типизация в действии</h3>

<p>Покажем, как одна переменная может менять тип в реальном сценарии:</p>

<pre><code>&lt;?php
// Боевой пример: обработка поля "количество" из формы
echo "&lt;h2&gt;Обработка данных из формы&lt;/h2&gt;";

// Симуляция данных из POST-запроса
$_POST['quantity'] = "5"; // из формы всегда приходит строка

$quantity = $_POST['quantity'];
echo "&lt;div class='example'&gt;";
echo "&lt;p&gt;1. Получили из формы: ";
var_dump($quantity); // string(1) "5"
echo "&lt;/p&gt;";

// Преобразуем в число для расчетов
$quantity = (int)$quantity;
echo "&lt;p&gt;2. Преобразовали в int: ";
var_dump($quantity); // int(5)
echo "&lt;/p&gt;";

// Добавляем +1 к количеству
$quantity = $quantity + 1;
echo "&lt;p&gt;3. Добавили 1: ";
var_dump($quantity); // int(6)
echo "&lt;/p&gt;";

// Формируем сообщение для пользователя
$quantity = "Новое количество: " . $quantity;
echo "&lt;p&gt;4. Сделали строкой для вывода: ";
var_dump($quantity); // string(31) "Новое количество: 6"
echo "&lt;/p&gt;";

echo "&lt;/div&gt;";

// Боевой пример 2: работа с настройками
echo "&lt;h3&gt;Работа с настройками сайта&lt;/h3&gt;";
echo "&lt;div class='example'&gt;";

$setting = "true"; // из конфигурационного файла или БД
echo "&lt;p&gt;Настройка из конфига: ";
var_dump($setting); // string(4) "true"

// Преобразуем в булево значение
$setting = ($setting === "true");
echo "&lt;p&gt;После преобразования: ";
var_dump($setting); // bool(true)

// Используем в условии
if ($setting) {
echo "&lt;p&gt;✅ Функция включена&lt;/p&gt;";
} else {
echo "&lt;p&gt;❌ Функция выключена&lt;/p&gt;";
}

echo "&lt;/div&gt;";
?&gt;</code></pre>

<div class="note">
<p><strong>🔄 Боевой сценарий:</strong> Именно так происходит обработка данных в реальных проектах. Данные из форм (всегда строки) преобразуются в нужные типы, меняются в процессе работы, и снова преобразуются для вывода.</p>
</div>
</div>

<div class="task">
<h3><span class="icon">🧪</span> Интерактивный демо: отладка реальной проблемы</h3>

<div class="interactive-demo">
<p>Представьте, что на сайте не работает расчет скидки. Давайте найдем ошибку:</p>

<div id="debug-output" class="example">
<button onclick="runDebugDemo()" class="button button--primary">Запустить демо отладки</button>
<div id="debug-result" style="margin-top: 20px; display: none;"></div>
</div>
</div>

<script>
function runDebugDemo() {
const resultDiv = document.getElementById('debug-result');
resultDiv.style.display = 'block';

resultDiv.innerHTML = `
<h4>🔍 Ситуация: скидка не применяется</h4>
<pre><code>&lt;?php
// Ошибочный код (как часто пишут новички)
$price = "1000"; // из формы пришла строка
$discount = "10"; // процент скидки

$final_price = $price - ($price * $discount / 100);
echo "Итоговая цена: $final_price"; // Выводит: 1000

// Давайте посмотрим, что происходит:
echo "\\n\\n=== ДЕБАГ ===";
var_dump($price);      // string(4) "1000"
var_dump($discount);   // string(2) "10"
var_dump($price * $discount); // int(10000) - ОПАСНО!

// Правильное решение:
$price = (float)$price;
$discount = (float)$discount;
$final_price = $price - ($price * $discount / 100);
echo "\\nПравильная цена: $final_price"; // Выводит: 900.0
?&gt;</code></pre>
<div class="note">
<p><strong>Вывод:</strong> Всегда проверяйте типы переменных, особенно когда работаете с пользовательским вводом или данными из базы. Используйте var_dump() для отладки!</p>
</div>
`;
}
</script>
</div>
</section>

<section class="section">
<h2 class="section-title">📋 Домашнее задание</h2>

<div class="homework">
<h3><span class="icon">✏️</span> Задание на день 2</h3>

<p>Создайте файл <code>homework_day2.php</code> и реализуйте следующие задачи:</p>

<ol>
<li><strong>Профиль пользователя соцсети:</strong>
<ul>
<li>Создайте переменные для хранения данных профиля: имя, фамилия, возраст, город, статус (онлайн/оффлайн), количество друзей, количество постов</li>
<li>Рассчитайте "активность" пользователя (посты/друзья)</li>
<li>Выведите красивый профиль с использованием HTML</li>
<li>Добавьте var_dump() для проверки типов всех переменных</li>
</ul>
</li>

<li><strong>Система рейтинга товаров:</strong>
<ul>
<li>Создайте переменные для товара: название, цена, количество отзывов, средний рейтинг (от 1 до 5)</li>
<li>Рассчитайте "популярность" товара (рейтинг * количество отзывов)</li>
<li>Добавьте систему скидок: если рейтинг > 4.5, скидка 15%, если > 4.0 - 10%, иначе 5%</li>
<li>Выведите карточку товара с итоговой ценой</li>
</ul>
</li>

<li><strong>Генератор квитанции об оплате:</strong>
<ul>
<li>Создайте переменные: номер квитанции, дата, услуга, сумма, комиссия, итог</li>
<li>Сформируйте текст квитанции в формате, готовом для печати</li>
<li>Создайте HTML-версию с оформлением для email</li>
<li>Используйте конкатенацию и интерполяцию строк</li>
</ul>
</li>

<li><strong>Бонус: Симуляция бага и его исправление:</strong>
<ul>
<li>Создайте сценарий, где из-за неправильных типов данных ломается расчет</li>
<li>Покажите, как с помощью var_dump() найти ошибку</li>
<li>Исправьте код и покажите правильный результат</li>
</ul>
</li>
</ol>

<div class="note">
<p><strong>💪 Усложненное задание:</strong> Попробуйте создать мини-проект "Личный бюджет", где переменные будут хранить доходы, расходы по категориям, и рассчитывать остаток на конец месяца.</p>
</div>
</div>
</section>

<section class="section">
<h2 class="section-title">🧠 Проверка понимания</h2>

<div class="quiz">
<h3><span class="icon">❓</span> Вопросы для самопроверки</h3>

<ol>
<li>Почему <code>$productPrice</code> лучше, чем <code>$pp</code>?</li>
<li>Что выведет <code>var_dump(0 == "0")</code> и почему это важно?</li>
<li>Как преобразовать строку "123.45" в число с плавающей точкой?</li>
<li>Почему в интернет-магазине цена хранится как float, а не int?</li>
<li>Что опасного в динамической типизации при работе с формами?</li>
<li>Как проверить, что переменная действительно содержит null, а не пустую строку?</li>
<li>Зачем использовать var_dump() вместо echo для отладки?</li>
<li>Почему <code>$count = "5" + "10"</code> даст 15, а не "510"?</li>
<li>Как правильно сложить строку "Цена: " и число 1000?</li>
<li>В чем разница между <code>$active = true</code> и <code>$active = "true"</code>?</li>
</ol>

<button onclick="toggleAnswers()" class="button button--primary" style="margin-top: 20px;">Показать ответы</button>

<div id="answers" style="display: none; margin-top: 20px; padding: 20px; background: var(--light-color); border-radius: var(--border-radius); border-left: 4px solid var(--primary-color);">
<h4>Ответы:</h4>
<ol>
<li><code>$productPrice</code> описывает содержимое, делает код читаемым для других разработчиков.</li>
<li><code>bool(true)</code> — PHP приводит типы. Опасно при строгих проверках (===).</li>
<li><code>$price = (float)"123.45";</code> или <code>$price = floatval("123.45");</code></li>
<li>Цены часто содержат копейки/центы (29.99), float хранит дробную часть.</li>
<li>Пользователь может отправить неожиданные данные (например, текст вместо числа).</li>
<li><code>is_null($var)</code> или <code>$var === null</code>. <code>==</code> считает пустую строку равной null!</li>
<li>var_dump() показывает тип данных, что критически важно для отладки.</li>
<li>PHP автоматически преобразует строки в числа при математических операциях.</li>
<li><code>"Цена: " . 1000</code> или <code>"Цена: $price"</code> (в двойных кавычках).</li>
<li>Первое — булево значение true, второе — строка "true". В условиях они ведут себя по-разному.</li>
</ol>
</div>
</div>
</section>

<section class="section resources">
<h2 class="section-title">🔗 Полезные ресурсы</h2>

<div class="resource-list">
<div class="resource-item">
<a href="https://www.php.net/manual/ru/language.variables.php" target="_blank">Официальная документация по переменным</a>
<p>Все тонкости работы с переменными в PHP</p>
</div>
<div class="resource-item">
<a href="https://www.php.net/manual/ru/language.types.php" target="_blank">Типы данных в PHP</a>
<p>Подробное описание всех типов данных</p>
</div>
<div class="resource-item">
<a href="https://www.php.net/manual/ru/function.var-dump.php" target="_blank">Документация var_dump()</a>
<p>Все параметры и возможности функции</p>
</div>
<div class="resource-item">
<a href="https://www.php.net/manual/ru/language.types.type-juggling.php" target="_blank">Преобразование типов</a>
<p>Как PHP автоматически меняет типы данных</p>
</div>
<div class="resource-item">
<a href="https://www.w3schools.com/php/php_variables.asp" target="_blank">W3Schools: Переменные PHP</a>
<p>Интерактивные примеры и упражнения</p>
</div>
</div>
</section>